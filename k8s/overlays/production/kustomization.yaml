apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
- ../../base

namespace: eac-prod

replicas:
- name: eac-backend
  count: 3
- name: eac-web-app
  count: 2
- name: eac-frontend
  count: 2

images:
- name: ghcr.io/emmanuel-soempit/eac-backend
  newTag: latest
- name: ghcr.io/emmanuel-soempit/eac-web-app
  newTag: latest
- name: ghcr.io/emmanuel-soempit/eac-frontend
  newTag: latest

resources:
- secrets/sealed-secrets.yaml
- secrets/configmap.yaml
- ingress.yaml

# Production: remove in-cluster postgres — use managed DB (RDS, Supabase, etc.)
patches:
- patch: |-
    $patch: delete
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: eac-postgres
- patch: |-
    $patch: delete
    apiVersion: v1
    kind: Service
    metadata:
      name: eac-postgres
- patch: |-
    $patch: delete
    apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      name: postgres-pvc
