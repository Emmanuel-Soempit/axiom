apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eac-ingress
  namespace: eac-prod
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
  - hosts:
    - soempit.com
    - portal.soempit.com
    - api.soempit.com
    secretName: eac-prod-tls
  rules:
  - host: soempit.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: eac-frontend
            port:
              number: 3000
  - host: portal.soempit.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: eac-web-app
            port:
              number: 3001
  - host: api.soempit.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: eac-backend
            port:
              number: 3333
