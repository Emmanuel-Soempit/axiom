apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eac-ingress
  namespace: eac-dev
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
  - hosts:
    - dev.eac.soempit.com
    - portal.dev.eac.soempit.com
    - api.dev.eac.soempit.com
    secretName: eac-dev-tls
  rules:
  - host: dev.eac.soempit.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: eac-frontend
            port:
              number: 3000
  - host: portal.dev.eac.soempit.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: eac-web-app
            port:
              number: 3001
  - host: api.dev.eac.soempit.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: eac-backend
            port:
              number: 3333
